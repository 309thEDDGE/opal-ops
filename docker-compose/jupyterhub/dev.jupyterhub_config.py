import os
from shared_jupyterhub_config import *

# Only config unique to a local dev setup should be placed here
# Nearly all config can be put in shared_jupyterhub_config.py
set_shared_traitlets(c)
