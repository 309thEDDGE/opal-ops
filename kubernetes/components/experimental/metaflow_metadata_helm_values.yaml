metaflow-service:
  metadatadb:
    password: postgres
    name: metaflow_metadata
    user: postgres
    host: database

  ingress:
    enabled: false

metaflow-ui:
  metadatadb:
    password: postgres
    name: metaflow_metadata
    user: postgres
    host: database

  ingress:
    enabled: true
    hosts:
      - host: metaflow-k8-experimental.opalacceptance.dso.mil

postgresql:
  # postgresql.enabled -- Flag to install Postgresql
  enabled: false